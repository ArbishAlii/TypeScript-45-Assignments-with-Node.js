var friendsName = ["Arbish", "Alina", "Khayyam", "Ibrahim"];
// made a variable with name (friends name) then take only (friend name) to call everyone one by one
friendsName.forEach(function (friendName) { return console.log("Hi, ".concat(friendName, ", How are you doing?")); });
